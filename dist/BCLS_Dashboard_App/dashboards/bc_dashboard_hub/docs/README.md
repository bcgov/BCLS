# BC Dashboard Hub

- Purpose: parent dashboard that embeds the standalone dashboards in `dashboards/`
- HTML entry point: `html/dashboard.html`
- Child dashboard paths are defined in the `SECTORS` registry and section iframes.
