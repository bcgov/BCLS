# Tourism Data Workspace

Canonical Tourism datasets are stored in:

- `data/sectors/tourism/`

This local folder currently has no runtime dataset files.

Notes:

- Tourism GDP (`36-10-0711-01`) is read live from StatCan API in the dashboard.
- Tourism business count/size mix (`33-10-1095-01`) is read live from StatCan API in the dashboard.
- Tourism labour (`14-10-0023-01`) is read live from StatCan API in the dashboard.
